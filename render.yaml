services:
  # Frontend Static Site (Angular App)
  - type: web
    name: baculao-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist/angular-21-auth-boilerplate
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
